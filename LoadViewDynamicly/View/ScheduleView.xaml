<UserControl x:Class="LoadViewDynamicly.View.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoadViewDynamicly.View"
             xmlns:vm="clr-namespace:LoadViewDynamicly.ViewModel"
             xmlns:igEditors="http://infragistics.com/Editors"
             mc:Ignorable="d" 
             xmlns:igDP="http://infragistics.com/DataPresenter"
             xmlns:igExcelExporter="http://infragistics.com/ExcelExporter"
             d:DesignHeight="300" d:DesignWidth="300">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="3*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid Margin="0,0,0,0" Grid.Row="0" Grid.Column="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"></ColumnDefinition>
                <ColumnDefinition Width="3*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>

            <Label Grid.Column="0" Content="Pick up Class" HorizontalAlignment="Left"   Margin="0,0,0,0" VerticalAlignment="Bottom" Width="Auto"/>
            <Label Grid.Column="1" Content="Teacher" HorizontalAlignment="Left"   Margin="0,0,0,0" VerticalAlignment="Bottom" Width="Auto"/>
            <Label Grid.Column="2" Content="Date" HorizontalAlignment="Left"   Margin="0,0,0,0" VerticalAlignment="Bottom" Width="Auto"/>
            <Label Grid.Column="3" Content="StartTime" HorizontalAlignment="Left"   Margin="0,0,0,0" VerticalAlignment="Bottom" Width="Auto"/>
            <Label Grid.Column="4" Content="EndTime" HorizontalAlignment="Left"   Margin="0,0,0,0" VerticalAlignment="Bottom" Width="Auto"/>


            <!--https://stackoverflow.com/questions/4902039/difference-between-selecteditem-selectedvalue-and-selectedvaluepath -->
            <ComboBox x:Name="classCombo"
                      ItemsSource="{Binding ClassTable}"                        
                      DisplayMemberPath="FullName"
                      SelectedValuePath="ID"                      
                      SelectedValue="{Binding Path=ClassId}"
                      Grid.Row="1"
                      Grid.Column="0"
                      Height="22"
                      />

            <ComboBox x:Name="teacherCombo"
                      ItemsSource="{Binding TeacherTable}"                        
                      DisplayMemberPath="FullName"
                      SelectedValuePath="ID"                      
                      SelectedValue="{Binding Path=TeacherId}"
                      Grid.Row="1"
                      Grid.Column="1"
                      Height="22"
                      />

            <igEditors:XamDateTimeEditor 
                 
                       Height="21" 
                       Name="scheduleDate"  
                       Grid.Row="1"
                       Grid.Column="2"
                       Text="{Binding Asof, UpdateSourceTrigger=PropertyChanged}"   
                       VerticalAlignment="Center"   />

            <TextBox x:Name="startTime" 
                     Text="{Binding ClassStartTime}"  
                     HorizontalAlignment="Left" 
                     Height="20" 
                     Margin="0,0,0,0"  
                     TextWrapping="Wrap"   
                     Width="100" 
                     ToolTip="Class Start Time" 
                     Cursor="Arrow"
                     Grid.Row="1"
                     Grid.Column="3"
                     Opacity="0.9"/>

            <TextBox x:Name="endTime" 
                     Text="{Binding ClassEndTime}"  
                     HorizontalAlignment="Left" 
                     Height="20" 
                     Margin="0,0,0,0"  
                     TextWrapping="Wrap"   
                     Width="100" 
                     ToolTip="Class Start Time" 
                     Cursor="Arrow"
                     Grid.Row="1"
                     Grid.Column="4"
                     Opacity="0.9"/>
            
            <Button Margin="0,0,-20,0"  
                    Background="LightBlue" 
                    Height="25" 
                    Width="Auto"
                    Grid.Row="1"
                    Grid.Column="5"                    
                    Command="{Binding LoadStudentCommand}" Padding="1">Load Students</Button>

        </Grid>




        <igDP:XamDataPresenter DataSource="{Binding Path=Members}"  Name="ScheduleGrid" Margin="0,0,0,0"   Grid.Row="1" Grid.Column="1" >


            <igDP:XamDataPresenter.Resources>
                <!-- This Style puts a CheckBox into the record selectors. -->
                <Style TargetType="{x:Type igDP:RecordSelector}"
                       BasedOn="{x:Null}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type igDP:RecordSelector}">
                                <CheckBox HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding Path=DataItem.IsChecked, Mode=TwoWay}" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <!-- This Style puts a CheckBox into the header area above the record selectors. -->
                <Style TargetType="{x:Type igDP:HeaderPrefixArea}"
                       BasedOn="{x:Null}">
                    <Setter Property="Visibility"
                            Value="Visible" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type igDP:HeaderPrefixArea}">
                                <CheckBox HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding Path=DataPresenter.DataContext.AllMembersAreChecked, Mode=TwoWay}" />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </igDP:XamDataPresenter.Resources>
            
            <igDP:XamDataPresenter.FieldLayouts>
                <igDP:FieldLayout>
                    <igDP:FieldLayout.Fields>
                        
                        <igDP:Field Name="ID" Label="ID" />
                        <igDP:Field Name="FullName" Label="FullName" />
                        <igDP:Field Name="Comment" Label="Comment" />
                        <igDP:Field Name="IsChecked" Label="IsChecked" Visibility="Collapsed" />
                    </igDP:FieldLayout.Fields>
                </igDP:FieldLayout>
            </igDP:XamDataPresenter.FieldLayouts>

            <igDP:XamDataPresenter.FieldLayoutSettings>
                <igDP:FieldLayoutSettings AutoGenerateFields="false" FilterUIType="FilterRecord"                                          
                                            HighlightAlternateRecords="True"
                                              HeaderPrefixAreaDisplayMode="FieldChooserButton"/>
            </igDP:XamDataPresenter.FieldLayoutSettings>

            

        </igDP:XamDataPresenter>

        <TextBox  Grid.Row="2" 
                Grid.Column="1"
                Text="Please enter comment for this class: " 
                Margin="0,0,0,0" VerticalAlignment="Bottom" Width="Auto"/>

        <Button Grid.Row="3"
                    Grid.Column="1"  
                    Click="OnShowRecords" 
                    Command="{Binding SaveAttendanceCommand}"
                   >
            Save Attendance
        </Button>

    </Grid>
</UserControl>

